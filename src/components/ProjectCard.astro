---
import type { MergedProject } from '../lib/github';
import { categoryLabels } from '../data/projects';
import { withBase } from '../lib/url';

interface Props {
  project: MergedProject;
}

const { project } = Astro.props;
const title = project.displayName;
---

<article class="group flex flex-col overflow-hidden rounded-xl border border-neutral-800 bg-neutral-900/50 transition-colors hover:border-emerald-500/30">
  {
    project.screenshot ? (
      <img
        src={withBase(project.screenshot)}
        alt={`Screenshot of ${title}`}
        class="h-48 w-full object-cover"
        loading="lazy"
      />
    ) : (
      <div class="flex h-48 items-center justify-center bg-neutral-800/50">
        <span class="text-4xl font-bold text-neutral-700">{title.charAt(0).toUpperCase()}</span>
      </div>
    )
  }
  <div class="flex flex-1 flex-col p-5">
    <div class="flex items-start justify-between gap-2">
      <div>
        <span class="text-xs font-medium text-emerald-400">{categoryLabels[project.category]}</span>
        <h3 class="mt-1 text-lg font-semibold text-neutral-100 group-hover:text-emerald-400 transition-colors">
          {title}
        </h3>
      </div>
      {
        project.stars > 0 && (
          <span class="flex items-center gap-1 text-xs text-neutral-500">
            <svg class="h-3 w-3" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" /></svg>
            {project.stars}
          </span>
        )
      }
    </div>
    <p class="mt-2 flex-1 text-sm text-neutral-400 line-clamp-3">{project.description}</p>
    {
      project.techStack.length > 0 && (
        <div class="mt-3 flex flex-wrap gap-1.5">
          {project.techStack.map((tech) => (
            <span class="rounded bg-neutral-800 px-2 py-0.5 text-xs text-neutral-400">{tech}</span>
          ))}
        </div>
      )
    }
    <div class="mt-4 flex gap-3">
      {
        project.category !== 'meta' && (
          <a
            href={project.htmlUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="text-sm font-medium text-emerald-400 hover:text-emerald-300 transition-colors"
          >
            GitHub &rarr;
          </a>
        )
      }
      {
        project.demoUrl && project.demoUrl !== '#' && (
          <a
            href={project.demoUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="text-sm font-medium text-neutral-400 hover:text-emerald-400 transition-colors"
          >
            {project.category === 'meta' ? 'Live Site' : 'Live Demo'} &rarr;
          </a>
        )
      }
      {
        project.category === 'meta' && (
          <a
            href={project.htmlUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="text-sm font-medium text-emerald-400 hover:text-emerald-300 transition-colors"
          >
            View Source &rarr;
          </a>
        )
      }
    </div>
  </div>
</article>
