---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-10">
  <h2 class="text-3xl font-bold tracking-tight text-neutral-100">{title}</h2>
  {subtitle && <p class="mt-2 text-neutral-400">{subtitle}</p>}
</div>
