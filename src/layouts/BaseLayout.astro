---
/**
 * @deprecated Use CinematicLayout directly. Kept as a thin alias for compatibility.
 */
import CinematicLayout from './CinematicLayout.astro';

interface Props {
  title?: string;
  description?: string;
}

const props = Astro.props as Props;
---

<CinematicLayout title={props.title} description={props.description}>
  <slot />
</CinematicLayout>
